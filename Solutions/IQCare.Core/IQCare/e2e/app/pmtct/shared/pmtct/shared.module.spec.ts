import { Pmtct\sharedModule } from './pmtct\shared.module';

describe('Pmtct\sharedModule', () => {
  let pmtct\sharedModule: Pmtct\sharedModule;

  beforeEach(() => {
    pmtct\sharedModule = new Pmtct\sharedModule();
  });

  it('should create an instance', () => {
    expect(pmtct\sharedModule).toBeTruthy();
  });
});
