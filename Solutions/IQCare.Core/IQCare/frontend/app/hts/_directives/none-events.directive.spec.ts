import { NoneEventsDirective } from './none-events.directive';

describe('NoneEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new NoneEventsDirective();
    expect(directive).toBeTruthy();
  });
});
