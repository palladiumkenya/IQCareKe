import { ClickNoneEventsDirectiveDirective } from './click-none-events-directive.directive';

describe('ClickNoneEventsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickNoneEventsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
